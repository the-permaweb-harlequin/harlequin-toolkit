{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "description": "AO process built with AssemblyScript",
  "type": "module",
  "scripts": {
    "prebuild": "tsc",
    "build": "asc --config asconfig.json",
    "build:debug": "asc --config asconfig.json --debug",
    "dev": "pnpm run build && pnpm run test",
    "test": "npm run build && tsx test/test.ts",
    "clean": "rm -rf build lib/dist",
    "deploy": "echo 'Deploy script not implemented yet'"
  },
  "keywords": [
    "ao",
    "arweave",
    "assemblyscript",
    "webassembly",
    "process"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@permaweb/ao-loader": "^0.0.49",
    "@types/node": "^20.0.0",
    "assemblyscript": "^0.27.0",
    "tsx": "^4.20.5",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "assemblyscript-json": "^1.1.0"
  }
}
