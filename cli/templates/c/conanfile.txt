[requires]
# JSON library for message parsing and response formatting
nlohmann_json/3.11.3

# Optional: Testing framework
gtest/1.14.0

# Optional: Logging library
spdlog/1.12.0

# Optional: String utilities
fmt/10.1.1

[generators]
CMakeDeps
CMakeToolchain

[options]
# JSON library options
nlohmann_json:multiple_headers=False

# GTest options (for testing)
gtest:shared=False
gtest:build_gmock=True

# spdlog options
spdlog:shared=False
spdlog:header_only=True

[imports]
# Copy shared libraries to bin folder (if any)
bin, *.dll -> ./bin
lib, *.dylib* -> ./bin
lib, *.so* -> ./bin

